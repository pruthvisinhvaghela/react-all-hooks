// import { useState } from "react";

const Test = () => {
  // const [data, setData] = useState(7);

  return (
    <>
      {/* <input
        type="text"
        onChange={(e) => {
          setData(e.target.value);
        }}
        name="fname"
        value={data}
      /> */}

      {/* <h1>you clicked {data} time</h1>
      <br />

      <button onClick={() => setData(data > 0 ? data - 1 : 0)}>click me</button> */}
    </>
  );
};

export default Test;
